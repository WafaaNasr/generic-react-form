import { TextFieldProps } from './TextField.types';

const data: TextFieldProps = {
    placeholder: 'user x',
    name: 'name',
    type: 'text',
    label: 'Name',
    onTextChange: null,
};

export default data;
